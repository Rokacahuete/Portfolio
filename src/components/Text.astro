---
const { class: classList, id } = Astro.props;
---

<p class=`text ${classList}` id={id ?? ""}>
    <slot />
</p>

<style>
	.text {
  		font-family: "Comic Sans MS", sans-serif;
		margin: 0px 50px 10px 50px;
	}
</style>